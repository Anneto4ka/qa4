// ДЗ8. Функции, введение
// Каждая задача - одна функция. Внутри можно использовать циклы, if else, но НЕ МЕТОДЫ!

// Ваше имя и фамилия: Hanna Skoropadska

// 1.У вас есть функция sum, которая складывает два числа.
// Для чисел 23 и 18 выведите результат ее работы в консоль.

function sum(a, b) {
  return a + b;
}
console.log(sum(23, 18));

// 2.Напишите функцию multiply, которая перемножит 3 числа: a, b, c.

function mult(a, b, c) {
  return a * b * c;
}
console.log(mult(3, 4, 5));

// 3.Напишите функцию isString, которая проверит, что передаваемое значение строка и выдаст true/false.
function isString(s) {
  return typeof s === 'string';
}
let s = '166023';
console.log(isString(s));

// 4.Напишите функцию (имя придумайте сами), которая будет принимать 2 числа
// и возвращать большее из них без использования методов Math.max или Math.min.

function bigNum(a, b) {
  return (a > b) ? a : b;
}
console.log(bigNum(46, 98));


// 5.Напишите функцию, которая принимает одно положительное целое число и возвращает
// массив с перечнем чисел от 0 до этого числа включительно с шагом единица.

function count(n) {
  let arr = [];
  for (let i = 0; i <= n; i++) {
    arr.push(i);
  }
  return arr;
}
console.log(count(7));


// 6.Напишите функцию, которая принимает массив чисел и возвращает их сумму.

function total(arr) {
    let sum = 0;
    for (let i = 0; i < arr.length; i++) {
      sum += arr[i];
    }
    return sum;
  }
  // arr = [3, 67, 24, 1, -10]
console.log(total([3, 67, 24, 1, -10]));
