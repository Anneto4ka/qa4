// ДЗ. Операторы

// Ваше имя и фамилия: Hanna Skoropadska


// 1. Объявите переменные a, b, c, d и присвойте им значения 243, 517, 'one', '2' соответственно.

let a = 243;
let b = 517;
let c = 'one';
let d = '2';

// 2. С помощью операторов +, * и заданных выше переменных выведите в консоль
//    число, строку, NaN (используйте любые комбинации переменных выше и знаков + и *).
//    В этом задании переменные не переприсваивайте, просто выводите в консоль!
//    (Что? Юля сказала писать выражения в console.log? 0_0)

console.log(a * b);
console.log(c + b);
console.log(c * b);

// 3. Преобразуйте переменную d к числу (переприсвойте), выведите в консоль.

d = +d;
console.log(d);
console.log(typeof d);

// 4. С помощью инкремента и дикремента увеличьте a на единицу, уменьшите b на единицу, выведите в консоль.
console.log(++a);
console.log(--b);
console.log('_____________');

// 5. Возведите d (помните, что это теперь число, да?) в шестую степень, выведите в консоль.

d = d ** 6;
console.log(d);
console.log('_____________');

// 6. Извлеките корень квадратный из d, выведите в консоль.

console.log(d ** 0.5);
console.log('_____________');

// 7. Задайте новую переменную, присвойте в нее целое число. Опишите процедуру сравнения:
//    Если это число четное, то увеличьте его на один (используйте инкремент, затем выведите в консоль).
//    В обратном случае уменьшите его на один (используйте декремент, затем выведите в консоль).

let e = 725;
if (e % 2 === 0) {
  e++;
} else {
  e--;
}
console.log(e);
console.log('_____________');


// 8. Задайте переменную dozen.
//    Задайте переменную z и присвойте в нее число 12.
//    С помощью какого оператора можно одновременно присвоить переменной dozen число 12 и увеличить z на единицу?

let dozen;
let z = 12;

dozen = z++;

console.log(dozen, z);



